name: SSH

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: true
jobs:
  build:
    runs-on: ubuntu-latest   
    permissions:
      actions: write
      contents: write
    steps:
    - name: 准备完成
      uses: actions/checkout@main
      
    - name: 运行       
      run: | 
        # mkdir ./releases/
        # mv ./sqlite3/shopee/采集箱/粉丝/br.db ./releases/shopee_gather_fans_br.db       
        # mv ./sqlite3/shopee/采集箱/粉丝/my.db ./releases/shopee_gather_fans_my.db
        # mv ./sqlite3/shopee/采集箱/粉丝/tw.db ./releases/shopee_gather_fans_tw.db

        
        # mkdir ./releases/s/shopee/采集箱/粉丝/
        # mv ./sqlite3/shopee/采集箱/粉丝/br.db ./releases/shopee/采集箱/粉丝/br.db
        
        # 7z x nextjs.7z
        # mv ./nextjs/plugins/ ./nextjs/public/plugins/
        # unzip ./nextjs.zip
        # unzip ./plugins.zip ./nextjs/
        
         
          # wget -O 1 https://github.com/rendie-com/rendie-com/releases/download/1/1.zip          
          # mkdir ../images/
          # cp image.tar ../images/image.tar
          # tar -czvf ../images/image.tar.gz image.tar     
          
    - name: SSH connection to Actions
      uses: P3TERX/ssh2actions@v1.0.0
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh  != 'false') || contains(github.event.action, 'ssh')
      
    - name: 部署next.js
      uses: JamesIves/github-pages-deploy-action@v4
      with: 
          branch: main
          folder: ./nextjs/
          target-folder: ./nextjs/
          
    - name: 自动发布固件到 Releases
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        name: sqlite3 文件发布
        tag_name: 1
        files: ./releases/* 
        
    - name: 删除运行记录
      uses: Mattraks/delete-workflow-runs@v2
      with:
        retain_days: 0
        keep_minimum_runs: 1
